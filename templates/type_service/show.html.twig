{% extends 'base.html.twig' %}

{% block title %}TypeService{% endblock %}

{% block body %}
    <h1>TypeService</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_service.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_service.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ type_service.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_service_index') }}">back to list</a>

    <a href="{{ path('type_service_edit', {'id': type_service.id}) }}">edit</a>

    {{ include('type_service/_delete_form.html.twig') }}
{% endblock %}
