<form method="post" action="{{ path('type_service_delete', {'id': type_service.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_service.id) }}">
    <button class="btn btn-danger">{{ 'delete.form'|trans }}</button>
</form>
