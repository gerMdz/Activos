{% extends 'base.html.twig' %}

{% block title %}ServiceManager{% endblock %}

{% block body %}
    <h1>ServiceManager</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ service_manager.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ service_manager.name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ service_manager.email }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ service_manager.phone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('service_manager_index') }}">back to list</a>

    <a href="{{ path('service_manager_edit', {'id': service_manager.id}) }}">edit</a>

    {{ include('service_manager/_delete_form.html.twig') }}
{% endblock %}
