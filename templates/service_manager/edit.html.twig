{% extends 'base.html.twig' %}

{% block title %} {{ 'ServiceManager'|trans }} {{ 'edit'|trans }}{% endblock %}

{% block body %}
    <div class="col-sm-11 mx-auto mt-3">
        <h4> {{ 'edit'|trans }} {{ 'ServiceManager'|trans }} </h4>

        {{ include('service_manager/_form.html.twig', {'button_label': 'button.update'|trans}) }}

        <a href="{{ path('service_manager_index') }}">
            {% include('default/_back.html.twig') %}
        </a>

        {{ include('service_manager/_delete_form.html.twig') }}
    </div>
{% endblock %}
