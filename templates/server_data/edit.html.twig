{% extends 'base.html.twig' %}

{% block title %}{{ 'edit'|trans }} {{ 'ServerData'|trans }}{% endblock %}

{% block body %}
    <div class="col-sm-11 mx-auto mt-3">
        <h4>{{ 'edit'|trans }} {{ 'ServerData'|trans }}</h4>

        {{ include('server_data/_form.html.twig', {'button_label': 'button.update'|trans}) }}

        <a href="{{ path('server_data_index') }}">
            {% include('default/_back.html.twig') %}
        </a>

        {{ include('server_data/_delete_form.html.twig') }}
    </div>
{% endblock %}
