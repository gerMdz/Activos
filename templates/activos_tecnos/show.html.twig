{% extends 'base.html.twig' %}

{% block title %}ActivosTecnos{% endblock %}

{% block body %}
    <h1>ActivosTecnos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ activos_tecno.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ activos_tecno.name }}</td>
            </tr>
            <tr>
                <th>Urlweb</th>
                <td>{{ activos_tecno.urlweb }}</td>
            </tr>
            <tr>
                <th>Plan</th>
                <td>{{ activos_tecno.plan }}</td>
            </tr>
            <tr>
                <th>ExpirationAt</th>
                <td>{{ activos_tecno.expirationAt ? activos_tecno.expirationAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('activos_tecnos_index') }}">back to list</a>

    <a href="{{ path('activos_tecnos_edit', {'id': activos_tecno.id}) }}">edit</a>

    {{ include('activos_tecnos/_delete_form.html.twig') }}
{% endblock %}
