<form method="post" action="{{ path('activos_tecnos_delete', {'id': activos_tecno.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ activos_tecno.id) }}">
    <button class="btn">Delete</button>
</form>
